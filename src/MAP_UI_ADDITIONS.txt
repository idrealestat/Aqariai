====================================
تعديلات واجهة الخريطة - نسخ ولصق مباشر
====================================

📍 **الملف:** /components/property-upload-complete.tsx

====================================
✅ التعديل 1: أضف بعد السطر 601 (بعد </button>)
====================================

        {spatialReport && (
          <button
            onClick={() => setShowSpatialReport(!showSpatialReport)}
            className="flex items-center gap-2 px-3 py-2 rounded-lg text-sm font-medium bg-blue-100 text-blue-800 border-2 border-blue-300 transition-all hover:bg-blue-200 w-full"
          >
            <Sparkles className="w-4 h-4" />
            {showSpatialReport ? 'إخفاء التقرير' : 'عرض التقرير المكاني'}
          </button>
        )}

====================================
✅ التعديل 2: أضف بعد السطر 605 (بعد </div> للإرشادات وقبل </div> النهائية)
====================================

      {showSpatialReport && spatialReport && (
        <SpatialIntelligenceReport 
          report={spatialReport}
          onClose={() => setShowSpatialReport(false)}
        />
      )}

====================================
📋 **الشكل النهائي يجب أن يكون:**
====================================

        </button>
        
        {/* ✨ زر التقرير المكاني - جديد */}
        {spatialReport && (
          <button
            onClick={() => setShowSpatialReport(!showSpatialReport)}
            className="flex items-center gap-2 px-3 py-2 rounded-lg text-sm font-medium bg-blue-100 text-blue-800 border-2 border-blue-300 transition-all hover:bg-blue-200 w-full"
          >
            <Sparkles className="w-4 h-4" />
            {showSpatialReport ? 'إخفاء التقرير' : 'عرض التقرير المكاني'}
          </button>
        )}
        
        <div className="text-xs text-gray-500 max-w-xs">
          💡 انقر على الخريطة لتحديد الموقع وسيتم ملء بيانات العنوان الوطني تلقائياً
        </div>
      </div>
      
      {/* ✨ لوحة التقرير المكاني - جديدة */}
      {showSpatialReport && spatialReport && (
        <SpatialIntelligenceReport 
          report={spatialReport}
          onClose={() => setShowSpatialReport(false)}
        />
      )}
      
    </div>
  );
};

====================================
🧪 **كيف تختبر:**
====================================

1. احفظ التعديلات
2. افتح الخريطة في صفحة "رفع عقار"
3. انقر على أي موقع في الخريطة
4. انتظر 1-2 ثانية للتحليل
5. سيظهر زر "عرض التقرير المكاني" 🌟 باللون الأزرق
6. اضغط عليه لفتح اللوحة الجانبية

====================================
🎯 **ماذا تتوقع أن ترى:**
====================================

✅ Console logs:
   🧠 Running Spatial Intelligence Analysis...
   ✅ Spatial Intelligence Report Generated: {...}

✅ في الواجهة:
   - زر أزرق جديد "عرض التقرير المكاني"
   - عند الضغط: لوحة جانبية من اليسار
   - تحتوي على:
     * درجة الجاذبية (0-100)
     * تعليق سوني الذكي
     * السياق العمراني
     * قرب الخدمات
     * تقييم المخاطر

====================================
